"""Skill-gap analysis core.

Deliberately free of any web-framework imports. Everything in here must be
runnable and testable from a plain Python REPL with no server running --
that constraint is what makes Stages 2-4 possible before Stage 5 exists.
"""

__version__ = "0.1.0"
